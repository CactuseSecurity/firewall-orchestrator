all:
  hosts:
    install-srv:
      ansible_connection: local
      ansible_host: localhost
    ui-srv:
      ansible_connection: ssh
      ansible_host: 192.168.121.2
  children:
    frontends:
      hosts:
        ui-srv:
    backendserver:
      hosts:
        install-srv:
    databaseserver:
      hosts:
        install-srv:
    apiserver:
      hosts:
        install-srv:
    importers:
      hosts:
        install-srv:
    middlewareserver:
      hosts:
        install-srv:
    sampleserver:
      hosts:
        install-srv:
    testservers:
      hosts:
        install-srv:
    logserver:
      hosts:
        install-srv:
