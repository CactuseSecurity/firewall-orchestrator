all:
  hosts:
    fworch-srv:
      ansible_connection: local
      ansible_host: localhost
  children:
    frontends:
      hosts:
        fworch-srv:
    backendserver:
      hosts:
        fworch-srv:
    databaseserver:
      hosts:
        fworch-srv:
    apiserver:
      hosts:
        fworch-srv:
    importers:
      hosts:
        fworch-srv:
    middlewareserver:
      hosts:
        fworch-srv:
    sampleserver:
      hosts:
        fworch-srv:
    testservers:
      hosts:
        fworch-srv:
