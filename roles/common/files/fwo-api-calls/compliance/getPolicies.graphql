query getPolicies {
  compliance_policy(where: {disabled: {_eq: false}}) {
    id
    name
    created_date
    disabled
    criteria: policy_criterions (where: {removed: {_is_null: true}}) {
      criterion: criterion {
        content
        criterion_type
        id
        name
      }
    }
  }
}
 
