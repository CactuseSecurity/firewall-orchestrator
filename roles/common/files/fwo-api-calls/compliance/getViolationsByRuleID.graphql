query getViolationsByRuleID($ruleId: bigint) {
  compliance_violation(where: { rule_id: { _eq: $ruleId } }) {
    id
    criterion_id
    details
    found_date
    policy_id
    removed_date
    risk_score
    rule_id
  }
}
