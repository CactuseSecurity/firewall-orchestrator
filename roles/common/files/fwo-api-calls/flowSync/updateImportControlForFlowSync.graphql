mutation updateImportControlForFlowSync($controlId: bigint!, $mgmId: Int!, $flowSyncDone: Boolean!) {
  update_import_control(
    where: { control_id: { _lte: $controlId }, mgm_id: { _eq: $mgmId } }
    _set: { flow_sync_done: $flowSyncDone }
  ) {
    affected_rows
  }
}