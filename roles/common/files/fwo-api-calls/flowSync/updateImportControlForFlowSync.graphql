mutation updateImportControlForFlowSync($controlId: bigint!, $flowSyncDone: Boolean!) {
  update_import_control(
    where: { control_id: { _eq: $controlId } }
    _set: { flow_sync_done: $flowSyncDone }
  ) {
    affected_rows
    returning {
      control_id
      flow_sync_done
    }
  }
}