mutation addImportForOwner($controlId: bigint!, $import_type_id: Int!) {
  insert_import_control_one(
    object: {
      control_id: $controlId
      import_type_id: $import_type_id
      changes_found: true
    }
  ) {
    control_id
    start_time
    changes_found
  }
}