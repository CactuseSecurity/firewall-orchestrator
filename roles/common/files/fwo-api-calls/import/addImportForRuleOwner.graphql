mutation addImportForRuleOwner($controlId: bigint!, $import_type_id: Int!) {
  insert_import_control_one(
    object: {
      control_id: $controlId, import_type_id: $import_type_id
    }
  ) {
    control_id
    start_time
  }
}
