query getPermittedOwnersForConnection($connectionId: Int!) {
  modelling_permitted_owners(where: {connection_id: {_eq: $connectionId}}) {
    app_id
    owner {
      id
      name
      external_app_id
    }
  }
}
