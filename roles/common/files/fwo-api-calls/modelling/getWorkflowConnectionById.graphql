query getWorkflowConnectionById($id: Int!) {
  modelling_connection(where: { id: { _eq: $id } }) {
    id
    app_id
    proposed_app_id
    name
    is_interface
    is_requested
    is_published
    ticket_id
    conn_prop
  }
}
