mutation resetConnectionRequestState(
  $id: Int!
  $connProp: String
  ) {
  update_modelling_connection_by_pk(
    pk_columns: { id: $id }
    _set: {
      is_requested: false
      ticket_id: null
      requested_on_fw: false
      conn_prop: $connProp
  }) {
    updatedId: id
  }
}
