query getOwnersWithNetworks {
  owner_list: owner(order_by: { name: asc }) {
    ...ownerDetails
    owner_networks(where: { is_deleted: { _eq: false } }) {
      id
      ip
      ip_end
      name
    }
  }
}