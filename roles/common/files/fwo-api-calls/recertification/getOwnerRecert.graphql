query getOwnerRecert ($ownerId: Int!) {
  owner_recertification (where: {owner_id: {_eq: $ownerId}}){
    id
    owner_id
    user_dn
    recertified
    recert_date
    comment
    next_recert_date
    report_id
  }
}
