query getOwnerRecerts ($ownerId: Int!) {
  owner_recertification (where: {owner_id: {_eq: $ownerId}} order_by: { recert_date: desc }){
    id
    owner_id
    user_dn
    recertified
    recert_date
    comment
    next_recert_date
    report_id
  }
}
