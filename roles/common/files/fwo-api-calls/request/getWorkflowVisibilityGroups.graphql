query getWorkflowVisibilityGroups {
  visibility_groups: request_workflow_visibility_group(order_by: {name: asc}) {
    id
    name
    description
    workflow_visibility_group_members(order_by: {member_dn: asc}) {
      visibility_group_id
      member_dn
    }
  }
}
