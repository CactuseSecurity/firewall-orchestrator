query GetRuleIdsByRuleOwner($ownerId: Int!, $owner_mapping_source: smallint!) {
    rule_owner(
        where: {
            owner_mapping_source_id: { _eq: $owner_mapping_source }
            owner_id: { _eq: $ownerId }
        }
    ) {
        rule_id
    }
}
