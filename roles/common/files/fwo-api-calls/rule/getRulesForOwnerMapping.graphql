query getRulesForOwnerMapping {
  rule(
    where: {
      removed: { _is_null: true },
      rule_custom_fields: { _is_null: false }
    }
  ) {
    rule_id
    rule_name
    rule_uid
    rule_metadatum {
      rule_metadata_id
    }
    rule_custom_fields
  }
}
