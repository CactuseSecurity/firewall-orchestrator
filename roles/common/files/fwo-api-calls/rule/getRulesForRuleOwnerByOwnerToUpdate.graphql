query getRulesForRuleOwnerByOwnerToUpdate ($ownerIds: [Int!]!) {
rule(
    where: {
      rule_owner: {
        owner_id: { _in: $ownerIds },
        removed: { _is_null: false }
        }
    }
  )
  {
      rule_id
      rule_name
      removed
      rule_custom_fields
  }
}