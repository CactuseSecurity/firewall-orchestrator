query getRulesForRuleOwnerCustomField {
  firewall_rule(where: { removed: { _is_null: true } }) {
    rule_id
    rule_name
    removed
    rule_uid
    rule_metadatum {
      rule_metadata_id
    }
    rule_custom_fields
  }
}
