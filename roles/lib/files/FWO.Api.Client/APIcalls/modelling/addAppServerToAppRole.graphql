mutation addAppServerToAppRole(
  $appServerId: bigint!
  $appRoleId: Int!
  ) {
  insert_modelling_nwobject_nwgroup(objects: {
    nwobject_id: $appServerId
    nwgroup_id: $appRoleId
  }) {
    returning {
      newId: nwobject_id
    }
  }
}
