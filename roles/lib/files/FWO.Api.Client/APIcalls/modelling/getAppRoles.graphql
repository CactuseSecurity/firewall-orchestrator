query getAppRoles ($appId: Int!){
  modelling_app_role (where: { app_id: { _eq: $appId } } order_by: { name: asc }){
    ...appRoleDetails
    app_servers: appserver_approles{
      app_server{
        ...appServerDetails
      }
    }

  }
}
