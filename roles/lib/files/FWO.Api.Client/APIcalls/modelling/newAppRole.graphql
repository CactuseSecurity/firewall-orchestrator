mutation newAppRole(
  $name: String
  $appId: Int
  $comment: String
  ) {
  insert_modelling_app_role(objects: {
    name: $name
    app_id: $appId
    comment: $comment
  }) {
    returning {
      newId: id
    }
  }
}
