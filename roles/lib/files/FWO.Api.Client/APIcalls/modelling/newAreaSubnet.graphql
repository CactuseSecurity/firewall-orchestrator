mutation newAreaSubnet(
  $name: String
  $ip: cidr
  $importSource: String
  ) {
  insert_modelling_nwobject(objects: {
    name: $name
    ip: $network
    import_source: $importSource
    is_deleted: false
    nw_type: 11
  }) {
    returning {
      newId: id
    }
  }
}
