mutation newAreaSubnet(
  $name: String
  $ip: cidr
  $importSource: String
  ) {
  insert_owner_network(objects: {
    name: $name
    ip: $ip
    import_source: $importSource
    is_deleted: false
    nw_type: 11
  }) {
    returning {
      newId: id
    }
  }
}
