mutation removeAppRoleFromConnection(
  $appRoleId: Int!
  $connectionId: Int!
  $connectionField: Int!
  ) {
  delete_modelling_approle_connection(where: {approle_id: {_eq: $appRoleId}, connection_id: {_eq: $connectionId}, connection_field: {_eq: $connectionField}}) {
    affected_rows
  }
}
