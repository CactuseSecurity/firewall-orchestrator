mutation removeAppServerFromAppRole(
  $appServerId: bigint!
  $appRoleId: Int!
  ) {
  delete_modelling_appserver_approle(where: {appserver_id: {_eq: $appServerId}, approle_id: {_eq: $appRoleId}}) {
    affected_rows
  }
}
