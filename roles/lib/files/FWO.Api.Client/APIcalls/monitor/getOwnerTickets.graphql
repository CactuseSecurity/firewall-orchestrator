
query getOwnerTickets ($ownerId: Int!) {
  owner_ticket (where: {owner_id: {_eq: $ownerId}} order_by: { ticket_id: desc }){
    owner: owner {
      id
      name
    }
    ticket: ticket {
      ...ticketDetails
    }
  }
}
