mutation newTicket(
  $title: String!, 
  $reason: String, 
  $state: Int,
  $requesterId: Int
  ) {
  insert_request_ticket(objects: {
    title: $title, 
    reason: $reason, 
    state_id: $state
    requester_id: $requesterId
  }) {
    returning {
      newId: id
    }
  }
}
