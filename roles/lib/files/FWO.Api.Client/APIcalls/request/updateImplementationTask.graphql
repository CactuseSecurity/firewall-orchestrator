mutation updateImplementationTask(
  $id: Int!
  $reqTaskId: Int!
  $implIaskNumber: Int
  $state: Int
  $device: Int
  $implAction: action_enum
  $ruleAction: Int
  $tracking: Int
  $handler: Int
  ) {
  update_implementation_task_by_pk(
    pk_columns: { id: $id }
    _set: {
    request_task_id: $reqTaskId
    implementation_task_number: $implIaskNumber
    state_id: $state
    device_id: $device
    implementation_action: $implAction
    rule_action: $ruleAction
    rule_tracking: $tracking
    current_handler: $handler
  }) {
    UpdatedId: id
  }
}
