mutation updateTicket(
  $id: Int!
  $title: String!, 
  $reason: String, 
  $state: Int,
  ) {
  update_request_ticket_by_pk(
    pk_columns: { id: $id }
    _set: {
    title: $title, 
    reason: $reason, 
    state_id: $state
  }) {
    UpdatedId: id
  }
}
