mutation updateTicketState(
  $id: Int!
  $state: Int
  $closed: timestamp
  ) {
  update_request_ticket_by_pk(
    pk_columns: { id: $id }
    _set: {
    state_id: $state
    date_completed: $closed
  }) {
    UpdatedId: id
  }
}
