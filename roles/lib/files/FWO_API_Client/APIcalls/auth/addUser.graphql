mutation addUser(
  $uiuser_username: String!
  $uuid: String!
) {
  insert_uiuser(
    objects: {
      uiuser_username: $uiuser_username
      uuid: $uuid
    }
  ) {
    returning {
       uiuser_id
       uiuser_username
       uuid
    }
  }
}
