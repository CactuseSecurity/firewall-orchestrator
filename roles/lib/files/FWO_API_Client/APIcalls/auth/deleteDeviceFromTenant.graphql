mutation delete_tenant_to_device_by_pk (
  $tenantId: Int!
  $deviceId: Int!
) {
  delete_tenant_to_device_by_pk (
    tenant_id: $tenantId
    device_id: $deviceId
  ) {
    DeletedId: device_id
  }
}

