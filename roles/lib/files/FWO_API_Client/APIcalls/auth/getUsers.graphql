query getUsers{
  uiuser {
    uiuser_id
    uuid
    uiuser_username
    uiuser_email
    tenant{
      tenant_id
      tenant_name
    }
    uiuser_language
    uiuser_last_login
    uiuser_last_password_change
    uiuser_password_must_be_changed
  }
}
