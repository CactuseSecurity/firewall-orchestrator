namespace FWO.Middleware.Server.Requests;

/// <summary>
/// Represents the RequestKeyDefinition record.
/// </summary>
public sealed record RequestKeyDefinition(string JsonName, string Description);
