[Service]
ExecStartPre=/usr/bin/mkdir -p /run/slapd
ExecStartPre=/usr/bin/chown openldap:openldap /run/slapd
ExecStart=
ExecStart=/usr/sbin/slapd -u {{ openldap_server_user }} -g {{ openldap_server_user }} -h "ldap://127.0.0.1 ldaps:///"
