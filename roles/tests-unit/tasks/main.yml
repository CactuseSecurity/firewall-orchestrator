- block:

  - name: copy test files to frontend target
    copy:
      src: FWO.Test
      dest: "{{ test_dir }}/csharp"
      owner: "{{ fworch_user }}"
      group: "{{ fworch_group }}"
    become: true

  - name: run csharp tests
    command: dotnet test
    args:
      chdir: "{{ csharp_test_start_dir }}"
    become: true
    become_user: "{{ fworch_user }}"
    register: csharp_tests
    ignore_errors: false
    environment: "{{ proxy_env }}"

  - name: show csharp test results in case of errors
    debug:
      var: csharp_tests
    when: csharp_tests.rc != 0

  - name: Run importer Python unit tests
    delegate_to: localhost
    environment:
      PYTHONPATH: "{{ playbook_dir }}:{{ playbook_dir }}/roles/importer/files/importer"
    args:
      chdir: "{{ playbook_dir }}/roles/importer/files/importer/test"
    command: >
      python3 -m unittest discover -s . -p "test_*.py" -v

  when: "'frontends' in group_names"
  # run only once (on frontends)
  