@using System.Text.Json
@using System.Security.Claims
@using FWO.Api.Client

@inject GlobalConfig globalConfig
@inject UserConfig userConfig
@inject ApiConnection apiConnection
@inject IJSRuntime jsRuntime
@inject NavigationManager navigationManager
@implements IDisposable

@if (InitComplete)
{
	<div class="position-sticky" style="z-index:15; top:0px;">
		<nav id="navbar" class="navbar navbar-expand-xl navbar-dark bg-blue shadow w-100">
			<a class="navbar-brand pad-10" href="#">
				@if (globalConfig.UseCustomLogo)
				{
					<img src="data:image/png;base64, @(globalConfig.CustomLogoData)" class="rounded cl-fixed-size" alt="">
				}
				else
				{
					<img src="/images/FWO_logo_navbar.png">
				}
				&nbsp;@userConfig.GetText("fworch_long")&nbsp;v@(globalConfig.ProductVersion)
			</a>

			<button class="navbar-toggler" type="button" @onclick="ToggleNavMenu">
				<span class="navbar-toggler-icon"></span>
			</button>

			<div class="navbar-collapse @NavMenuCollapseClass">
				<ul class="@NavPaddingCollapseClass d-flex flex-wrap navbar-nav align-items-start flex-row">
					@if (availableModules.Contains(Module.Report))
					{
						<ExecutionModeAuthorizeView Roles="@($"{Roles.Admin}, {Roles.FwAdmin}, {Roles.Reporter}, {Roles.ReporterViewAll}, {Roles.Auditor}, {Roles.Modeller}")">
							<li class="nav-item px-2">
								<a href="@("/" + PageName.ReportGeneration)" class="nav-link @(navigationManager.Uri.Contains("/report/") ? "active" : "")">
									<span class="@Icons.Reporting"></span> @(userConfig.GetText("reporting"))
								</a>
							</li>
						</ExecutionModeAuthorizeView>
					}
					@if (availableModules.Contains(Module.Workflow))
					{
						<ExecutionModeAuthorizeView Roles="@($"{Roles.Admin}, {Roles.WorkflowRolesList}, {Roles.FwAdmin}, {Roles.Auditor}")">
							<li class="nav-item px-2">
								<NavLink class="nav-link" href="@firstWorkflowPage">
									<span class="@Icons.Workflow"></span> @(userConfig.GetText("workflow"))
								</NavLink>
							</li>
						</ExecutionModeAuthorizeView>
					}
					@if (availableModules.Contains(Module.Recertification) && userConfig.RecertificationMode == RecertificationMode.RuleByRule)
					{
						<ExecutionModeAuthorizeView Roles="@($"{Roles.Admin}, {Roles.Modeller}, {Roles.Recertifier}, {Roles.Auditor}")">
							<li class="nav-item px-2">
								<NavLink class="nav-link" href="@("/" + PageName.Certification)">
									<span class="@Icons.Recertification"></span> @(userConfig.GetText("recertification"))
								</NavLink>
							</li>
						</ExecutionModeAuthorizeView>
					}
					@if (availableModules.Contains(Module.Modelling))
					{
						<ExecutionModeAuthorizeView Roles="@($"{Roles.Admin}, {Roles.Modeller}, {Roles.Auditor}")">
							<li class="nav-item px-2">
								<NavLink class="nav-link" href="@("/" + PageName.Modelling)">
									<span class="@Icons.Modelling"></span> @(userConfig.GetText("modelling"))
								</NavLink>
							</li>
						</ExecutionModeAuthorizeView>
					}
					@if (availableModules.Contains(Module.NetworkAnalysis))
					{
						<ExecutionModeAuthorizeView Roles="@($"{Roles.Admin}, {Roles.FwAdmin}, {Roles.Planner}, {Roles.Auditor}")">
							<li class="nav-item px-2">
								<NavLink class="nav-link" href="/network_analysis">
									<span class="@Icons.NetworkAnalysis"></span> @(userConfig.GetText("network_analysis"))
								</NavLink>
							</li>
						</ExecutionModeAuthorizeView>
					}
					@if (availableModules.Contains(Module.Compliance))
					{
						<ExecutionModeAuthorizeView Roles="@($"{ComplianceRoles()}")">
							<li class="nav-item px-2">
								<NavLink class="nav-link" href="/compliance/policies">
									<span class="@Icons.Compliance"></span> @(userConfig.GetText("compliance"))
								</NavLink>
							</li>
						</ExecutionModeAuthorizeView>
					}
				</ul>
				<ul class="@NavPaddingCollapseClass d-flex flex-wrap navbar-nav align-items-end ms-auto flex-row">
					<li class="nav-item px-2">
						@* d-flex align-items-center *@
						<NavLink class="nav-link" href="/monitoring">
							<span class="@Icons.Monitoring">
								@if (ShowAlert)
								{
									<span class="@Icons.Alarm" style="color: red; margin-left: 5px; width: 15px; height: 10px;"></span>
								}
							</span>
							@(userConfig.GetText("monitoring"))
						</NavLink>
					</li>
					<li class="nav-item px-2">
						<NavLink class="nav-link" href="/settings">
							<span class="@Icons.Settings"></span> @(userConfig.GetText("settings"))
						</NavLink>
					</li>
					<li class="nav-item px-2">
						<HelpLink> @userConfig.GetText("help")</HelpLink>
					</li>
					<li class="nav-item px-2">
						<NavLink class="nav-link" href="/settings/user">
							<span class="@Icons.User"></span> @(GetUserNameDisplay())
						</NavLink>
					</li>
				</ul>
			</div>
		</nav>
	</div>
}

@code
{
    [CascadingParameter]
    private Task<AuthenticationState>? authenticationStateTask { get; set; }

    [CascadingParameter]
    Action<Exception?, string, string, bool> DisplayMessageInUi { get; set; } = DefaultInit.DoNothing;

    [Parameter]
    public bool ShowAlert { get; set; }

    [Parameter]
    public EventCallback<bool> ShowAlertChanged { get; set; }

    bool collapseNavMenu = true;

    string NavMenuCollapseClass => (collapseNavMenu ? " collapse" : "");
    string NavPaddingCollapseClass => (collapseNavMenu ? "" : " px-3 py-1");

    string firstWorkflowPage = "";
    private StateMatrix stateMatrix = new();
    private List<Module> availableModules { get; set; } = [];
    private bool InitComplete = false;
    private bool disposed = false;
    private string displayedExecutionMode = GlobalConst.kUserRolesSelection;


    protected override async Task OnInitializedAsync()
    {
        try
        {
            userConfig.OnChange += OnChange;
            apiConnection.OnExecutionModeChanged += OnExecutionModeChanged;
            displayedExecutionMode = userConfig.ExecutionMode;
            availableModules = string.IsNullOrEmpty(userConfig.AvailableModules) ? [.. Enum.GetValues(typeof(Module)).Cast<Module>()]
                : JsonSerializer.Deserialize<List<Module>>(userConfig.AvailableModules) ?? throw new JsonException("Config data could not be parsed.");

            if (RequiresWorkflowStateInitialization())
            {
                if (authenticationStateTask == null)
                {
                    await stateMatrix.Init(WorkflowPhases.request, apiConnection);
                }
                else
                {
                    await apiConnection.RunWithWorkflowRole(authenticationStateTask.Result.User,
                        async () => await stateMatrix.Init(WorkflowPhases.request, apiConnection));
                }

                if (disposed)
                {
                    return;
                }

                ConfigureFirstWorkflowPage();
            }

            if (disposed)
            {
                return;
            }
        }
        catch (ObjectDisposedException)
        {
            return;
        }
        catch (Exception exception)
        {
            if (!disposed)
            {
                DisplayMessageInUi(exception, GetTextOrDefault("init_environment"), "", true);
            }
        }
        InitComplete = true;
    }

    private void OnExecutionModeChanged(object? sender, string executionMode)
    {
        if (disposed)
        {
            return;
        }

        _ = InvokeAsync(() =>
        {
            if (disposed)
            {
                return;
            }

            displayedExecutionMode = executionMode;
            firstWorkflowPage = "";
            if (stateMatrix.PhaseActive.Count > 0)
            {
                ConfigureFirstWorkflowPage();
            }
            StateHasChanged();
        });
    }

    private void ConfigureFirstWorkflowPage()
    {
        if (CanUseAnyRole(Roles.Admin, Roles.FwAdmin, Roles.Auditor))
        {
            firstWorkflowPage = "/request/ticketsoverview";
        }
        else if (stateMatrix.PhaseActive[WorkflowPhases.request] && CanUseAnyRole(Roles.Requester))
        {
            firstWorkflowPage = "/request/tickets";
        }
        else if (stateMatrix.PhaseActive[WorkflowPhases.approval] && CanUseAnyRole(Roles.Approver))
        {
            firstWorkflowPage = "/request/approvals";
        }
        else if (stateMatrix.PhaseActive[WorkflowPhases.planning] && CanUseAnyRole(Roles.Planner))
        {
            firstWorkflowPage = "/request/plannings";
        }
        else if (stateMatrix.PhaseActive[WorkflowPhases.implementation] && CanUseAnyRole(Roles.Implementer))
        {
            firstWorkflowPage = "/request/implementations";
        }
        else if (stateMatrix.PhaseActive[WorkflowPhases.review] && CanUseAnyRole(Roles.Reviewer))
        {
            firstWorkflowPage = "/request/reviews";
        }
    }
    
    void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

    private void OnChange(Config config, ConfigItem[] configItems)
    {
        if (disposed)
        {
            return;
        }

        availableModules = string.IsNullOrEmpty(userConfig.AvailableModules) ? [.. Enum.GetValues(typeof(Module)).Cast<Module>()]
            : JsonSerializer.Deserialize<List<Module>>(userConfig.AvailableModules) ?? throw new JsonException("Config data could not be parsed.");
        displayedExecutionMode = userConfig.ExecutionMode;
        _ = InvokeAsync(() =>
        {
            if (!disposed)
            {
                StateHasChanged();
            }
        });
    }

    private bool RequiresWorkflowStateInitialization()
    {
        return CanUseAnyRole(WorkflowNavigationRoles());
    }

    private bool CanUseAnyRole(params string[] targetRoles)
    {
        return ExecutionModeHelper.HasAnyRoleInExecutionMode(userConfig.User.Roles, displayedExecutionMode, targetRoles);
    }

    private static string[] WorkflowNavigationRoles()
    {
        return [Roles.Admin, Roles.FwAdmin, Roles.Requester, Roles.Approver, Roles.Planner, Roles.Implementer, Roles.Reviewer, Roles.Auditor];
    }

    private string ComplianceRoles()
    {
        string workflowRoles = userConfig.ReqShowCompliance ? ", " + Roles.WorkflowRolesList : "";
        return $"{Roles.Admin}, {Roles.FwAdmin}, {Roles.ReporterViewAll}, {Roles.Reporter}, {Roles.Auditor}{workflowRoles}";
    }

    private string GetUserNameDisplay()
    {
        string displayName = userConfig.User.Name.ToUpper();
        if (!ExecutionModeHelper.ShouldShowExecutionModeSelection(userConfig.User.Roles))
        {
            return displayName;
        }

        string executionMode = ExecutionModeHelper.GetSelectedExecutionMode(userConfig.User.Roles, displayedExecutionMode);
        return $"{displayName} ({GetExecutionModeText(executionMode)})";
    }

    private string GetExecutionModeText(string executionMode)
    {
        return executionMode == GlobalConst.kUserRolesSelection ? userConfig.GetText("user_roles") : executionMode;
    }

    private string GetTextOrDefault(string key)
    {
        try
        {
            return userConfig.GetText(key);
        }
        catch (ObjectDisposedException)
        {
            return key;
        }
    }

    public void Dispose()
    {
        disposed = true;
        userConfig.OnChange -= OnChange;
        apiConnection.OnExecutionModeChanged -= OnExecutionModeChanged;
    }
}
