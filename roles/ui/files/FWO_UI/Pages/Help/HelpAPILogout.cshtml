@page "/help/API/logout"
@{
    Layout = "HelpLayout";
}
@section sidebar{
    @{ 
        await Html.RenderPartialAsync("HelpAPISidebar.cshtml"); 
    }
}

<div class="p-2">
    <h3>Logout</h3>
        There is no logout functionality. When the JWT becomes invalid, API simply can no longer be made with this JWT.
</div>
