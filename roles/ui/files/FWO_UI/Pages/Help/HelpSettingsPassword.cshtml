@page "/help/settings/password"
@{
    Layout = "HelpLayout";
}
@section sidebar{
    @{ 
        await Html.RenderPartialAsync("HelpSettingsSidebar.cshtml"); 
    }
}
@using FWO.ApiConfig
@inject UserConfig userConfig

<div class="p-2">
    <h3>@(userConfig.GetText("password"))</h3>
    @(Html.Raw(userConfig.GetText("H5451")))
</div>
